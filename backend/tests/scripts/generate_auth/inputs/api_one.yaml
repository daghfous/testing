openapi: 3.0.0

info:
  version: 1.0.0
  title: Api One
  x-auth:
    prefix: apione
    label: Api One
    scope_descriptions:
      administrator: Scope for administrator users on api one
      guest: Scope for guest users on api one

paths:
  /one:
    put:
      x-scopes: ["administrator"]
      summary: Put Test
      description: Put Test description
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
          200:
            description: OK
    get:
      x-scopes: ["administrator", "guest"]
      summary: Get Test
      description: Get Test description
      responses:
        200:
          description: OK